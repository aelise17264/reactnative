import React from 'react';
import { Text, StyleSheet, View, Button } from 'react-native';

export default CounterScreen = () => {
    return <Text>This is the counter screen</Text>

};

const styles = StyleSheet.create({

})